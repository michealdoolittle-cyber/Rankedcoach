This is intended for the framework of the Valorant Improvement Platform.

Local review:

- Run `powershell -ExecutionPolicy Bypass -File .\scripts\start-review.ps1`
- Open `http://127.0.0.1:3000`
- This serves the current `public/` build for visual review before pushing another Cloudflare deploy
